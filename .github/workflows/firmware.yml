name: Firmware

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: firmware-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-release:
    runs-on: ubuntu-24.04

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run host tests
        run: |
          mkdir -p build
          python -m py_compile tools/package_firmware.py
          python -m py_compile tools/firmware_metrics.py tools/verify_third_party.py
          python tools/verify_third_party.py \
            --manifest third_party/manifest.json \
            --workflow .github/workflows/firmware.yml \
            --notices THIRD_PARTY_NOTICES.md \
            --project-license LICENSE \
            --repository-root .
          python -c "from pathlib import Path; [compile(path.read_text(encoding='utf-8'), str(path), 'exec') for path in Path('micropython/vfs').glob('*.py')]"
          python tests/micropython_supervisor_test.py
          g++ -std=c++17 -Wall -Wextra -Werror \
            tests/host_tests.cpp \
            firmware/CardputerAssistant/src/text_utils.cpp \
            firmware/CardputerAssistant/src/provider_profiles.cpp \
            firmware/CardputerAssistant/src/audio_utils.cpp \
            firmware/CardputerAssistant/src/document_reader.cpp \
            firmware/CardputerAssistant/src/instruction_policy.cpp \
            firmware/CardputerAssistant/src/pending_tool_preview.cpp \
            firmware/CardputerAssistant/src/tool_catalog.cpp \
            firmware/CardputerAssistant/src/tool_policy.cpp \
            firmware/CardputerAssistant/src/tool_policy_codec.cpp \
            firmware/CardputerAssistant/src/offline_tools.cpp \
            firmware/CardputerAssistant/src/ssh_terminal.cpp \
            -o build/host-tests
          ./build/host-tests
          node tests/web_console_ui_test.mjs

      - name: Install Arduino CLI
        uses: arduino/setup-arduino-cli@v2

      - name: Install M5Stack ESP32 core
        run: |
          arduino-cli config init --overwrite
          arduino-cli config add board_manager.additional_urls \
            https://static-cdn.m5stack.com/resource/arduino/package_m5stack_index.json
          arduino-cli core update-index
          arduino-cli core install m5stack:esp32@3.2.1

      - name: Fetch pinned libraries
        run: |
          mkdir -p .ci-libs
          git clone --filter=blob:none https://github.com/m5stack/M5Cardputer.git .ci-libs/M5Cardputer
          git -C .ci-libs/M5Cardputer checkout f1392858b9994c3547120e602a57d3553d16ab01
          git clone --filter=blob:none https://github.com/m5stack/M5Unified.git .ci-libs/M5Unified
          git -C .ci-libs/M5Unified checkout 774d920cd6851a5231748b56ece1b073645f313f
          git clone --filter=blob:none https://github.com/m5stack/M5GFX.git .ci-libs/M5GFX
          git -C .ci-libs/M5GFX checkout 93b480bb349749202c8a2a953065c8ae95f58320
          git clone --branch v7.2.1 --depth 1 https://github.com/bblanchon/ArduinoJson.git .ci-libs/ArduinoJson
          git clone --filter=blob:none https://github.com/libssh2/libssh2.git .ci-libssh2-source
          git -C .ci-libssh2-source checkout a312b43325e3383c865a87bb1d26cb52e3292641
          python tools/prepare_libssh2.py .ci-libssh2-source .ci-libs/libssh2-arduino

      - name: Compile Cardputer ADV firmware
        run: |
          mkdir -p build/release dist
          arduino-cli compile \
            --fqbn "m5stack:esp32:m5stack_cardputer:FlashSize=8M,PartitionScheme=custom" \
            --libraries .ci-libs \
            --output-dir build/release \
            firmware/CardputerAssistant

      - name: Measure firmware memory
        run: |
          SIZE_TOOL="$(find "$HOME/.arduino15/packages/m5stack/tools/esp-x32" -type f -name xtensa-esp-elf-size -print -quit)"
          test -x "$SIZE_TOOL"
          python tools/firmware_metrics.py \
            --size-tool "$SIZE_TOOL" \
            --elf build/release/CardputerAssistant.ino.elf \
            --app-bin build/release/CardputerAssistant.ino.bin \
            --partitions firmware/CardputerAssistant/partitions.csv \
            --baseline ci/firmware-baseline-v1.12.0.json \
            --budget ci/firmware-budget.json \
            --output build/release/firmware-metrics.json

      - name: Upload firmware memory report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: CardMind-firmware-metrics
          path: build/release/firmware-metrics.json
          if-no-files-found: warn

      - name: Package CardMind and MicroPython
        run: |
          curl --fail --location --silent --show-error \
            https://micropython.org/resources/firmware/ESP32_GENERIC_S3-20260406-v1.28.0.app-bin \
            --output build/release/micropython.app-bin
          echo "931f47de3076f51f386db204c2cc710d0ac42480ded50b1298cfb860e1bc1bfa  build/release/micropython.app-bin" \
            | sha256sum --check --strict
          MKLITTLEFS="$(find "$HOME/.arduino15/packages/m5stack/tools/mklittlefs" -type f -name mklittlefs -print -quit)"
          test -x "$MKLITTLEFS"
          "$MKLITTLEFS" -c micropython/vfs -b 4096 -p 32 -s 131072 build/release/micropython-vfs.bin
          "$MKLITTLEFS" -l -b 4096 -p 32 -s 131072 build/release/micropython-vfs.bin \
            | grep -F '/cardmind_supervisor.py'
          python tools/package_firmware.py \
            --cardmind-merged build/release/CardputerAssistant.ino.merged.bin \
            --cardmind-app build/release/CardputerAssistant.ino.bin \
            --micropython-app build/release/micropython.app-bin \
            --vfs-image build/release/micropython-vfs.bin \
            --output-dir dist
          curl --fail --location --silent --show-error \
            https://static-cdn.m5stack.com/resource/arduino/m5stack-3.2.1.zip \
            --output dist/CardMind-m5stack-esp32-3.2.1-source.zip
          echo "d1e647456f4b6f050a727ad5aac4cd4e4eded0911f80fb4786cdd4dce722cd46  dist/CardMind-m5stack-esp32-3.2.1-source.zip" \
            | sha256sum --check --strict
          ESP_IDF_COMMIT="2f7dcd862a62ec7380abe3e64c952d8038661463"
          grep -F "esp-idf: release/v5.4 ${ESP_IDF_COMMIT:0:10}" \
            "$HOME/.arduino15/packages/m5stack/tools/esp32-arduino-libs/idf-release_v5.4-2f7dcd86-v1/versions.txt"
          zip -r dist/CardMind-third-party-licenses.zip \
            LICENSE THIRD_PARTY_NOTICES.md third_party
          (
            cd dist
            sha256sum \
              CardMind-m5stack-esp32-3.2.1-source.zip \
              CardMind-third-party-licenses.zip \
              >> SHA256SUMS.txt
          )

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: CardMind-cardputer-adv
          path: |
            dist/CardMind-cardputer-adv.bin
            dist/CardMind-cardputer-adv-full.bin
            dist/SHA256SUMS.txt
            dist/CardMind-third-party-licenses.zip
            dist/CardMind-m5stack-esp32-3.2.1-source.zip
            build/release/firmware-metrics.json
            THIRD_PARTY_NOTICES.md
            third_party/manifest.json
          if-no-files-found: error

      - name: Publish version release
        if: github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION="$(sed -n 's/.*kFirmwareVersion = "\([^"]*\)".*/\1/p' firmware/CardputerAssistant/CardputerAssistant.ino)"
          test -n "$VERSION"
          TAG="v$VERSION"
          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" \
              dist/CardMind-cardputer-adv.bin \
              dist/CardMind-cardputer-adv-full.bin \
              dist/SHA256SUMS.txt \
              dist/CardMind-third-party-licenses.zip \
              dist/CardMind-m5stack-esp32-3.2.1-source.zip \
              build/release/firmware-metrics.json \
              THIRD_PARTY_NOTICES.md \
              third_party/manifest.json \
              --clobber
          else
            gh release create "$TAG" \
              dist/CardMind-cardputer-adv.bin \
              dist/CardMind-cardputer-adv-full.bin \
              dist/SHA256SUMS.txt \
              dist/CardMind-third-party-licenses.zip \
              dist/CardMind-m5stack-esp32-3.2.1-source.zip \
              build/release/firmware-metrics.json \
              THIRD_PARTY_NOTICES.md \
              third_party/manifest.json \
              --target "$GITHUB_SHA" \
              --title "CardMind $TAG" \
              --generate-notes
          fi
